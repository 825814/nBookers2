<p id="notice"><%= notice %></p>
  <h1>Books</h1>
  <div class="container">
    <div class="row">

    <div class="col-lg-4">
      <h2>User info</h2>


      <h2>New book</h2>

    <%= form_with model: @book do |f| %>


    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>

    <div class="field">
      <%= f.label :opinion %><br>
      <%= f.text_area :opinion, autofocus: true, autocomplete: "opinion" %>
    </div>

    <div>
      <%= link_to 'Create Book', book_path(@book), class: "btn btn-success" %>
    </div>
    <% end %>

    </div>

    <div class="col-lg-8">

      <table class="table table-striped">
        <thead>
          <tr>
            <th></th>
            <th>title</th>
            <th>opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @books.each do |book| %>
            <tr>
              <td><%= post.image %></td>
              <td><%= post.title %></td>
              <td><%= post.opinion %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>









